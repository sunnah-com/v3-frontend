/**
 * Re-export the useAuth hook from the auth provider
 * This makes it easier to import the hook from a single location
 */

import { useAuth } from '../components/auth/auth-provider';

export { useAuth };
